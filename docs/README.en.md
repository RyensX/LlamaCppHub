# LlamaCppHub

[中文](../README.md) | **English**

LlamaCppHub is a user interface shell for `llama.cpp`. It helps you manage multiple configurations and run local `llama.cpp` inference more conveniently.

## Features

- **Configuration Management**
  - Manage multiple configurations
  - Edit and duplicate configurations easily
- **llama.cpp Settings**
  - Set a global `llama.cpp` executable
  - Override the `llama.cpp` executable per configuration
- **Launch Arguments**
  - Configure official arguments quickly
  - Add raw custom arguments for features from different `llama.cpp` branches
  - Run pre-start commands
  - Preview the final service startup command
- **Instance Runtime**
  - Start and stop local `llama.cpp` instances
  - View instance PID, runtime status, and live logs
  - Open Web Chatbox quickly
- **Resource Monitoring**
  - Display CPU, GPU, RAM, and VRAM usage while a configuration is running
  - Observe resource changes with trend charts
- **Desktop Experience**
  - System tray and background running
  - Chinese and English interface
  - Light and dark themes

## Screenshots

<table>
  <tr>
    <td width="50%">
      <strong>Local Configuration</strong><br>
      <img src="../docs/image/image1.png" alt="Local Configuration" width="100%">
    </td>
    <td width="50%">
      <strong>Configuration Overview</strong><br>
      <img src="../docs/image/image2.png" alt="Configuration Overview" width="100%">
    </td>
  </tr>
  <tr>
    <td width="50%">
      <strong>Arguments and Descriptions</strong><br>
      <img src="../docs/image/image3.png" alt="Arguments and Descriptions" width="100%">
    </td>
    <td width="50%">
      <strong>Custom Arguments</strong><br>
      <img src="../docs/image/image4.png" alt="Custom Arguments" width="100%">
    </td>
  </tr>
  <tr>
    <td width="50%">
      <strong>Runtime Monitoring, Resource Trends, and Live Logs</strong><br>
      <img src="../docs/image/image5.png" alt="Runtime Monitoring, Resource Trends, and Live Logs" width="100%">
    </td>
    <td width="50%"></td>
  </tr>
</table>

## Tech Stack

- **Desktop Framework**: Tauri 2
- **Frontend**: React 18, TypeScript, Vite
- **Backend**: Rust
- **Styling**: Tailwind CSS
- **State Management**: Zustand
- **Internationalization**: i18next, react-i18next
- **Charts**: Recharts
- **Icons**: lucide-react

## Requirements

Before development or building from source, prepare:

- Node.js and npm
- Rust toolchain
- System dependencies required by Tauri 2
- A usable `llama.cpp server` executable

> Tauri system dependencies vary by operating system. Please install the dependencies required for your development platform.

## Quick Start

Install dependencies:

```bash
npm install
```

Start the desktop app in development mode:

```bash
npm run tauri:dev
```

If you only need to start the frontend development server:

```bash
npm run dev
```

## Scripts

| Command | Description |
| --- | --- |
| `npm run dev` | Start the Vite frontend development server |
| `npm run build` | Run TypeScript checks and build frontend assets |
| `npm run tauri` | Invoke the Tauri CLI |
| `npm run tauri:dev` | Start the desktop app in development mode |
| `npm run tauri:build` | Build production desktop artifacts |

## Production Build

Run:

```bash
npm run tauri:build
```

Build artifacts are generated by Tauri. The output directory and package format depend on your operating system and the bundle configuration in `src-tauri/tauri.conf.json`.

## Basic Workflow

1. Open LlamaCppHub.
2. Configure the global `llama.cpp server` executable path in Settings.
3. Create a new configuration, then set its name, model file path, and launch arguments.
4. Add pre-start commands or custom arguments if needed.
5. Confirm the final startup command in the command preview.
6. Start the local server instance for the configuration.
7. View live logs, resource usage, and trend charts on the detail page.
8. Stop the instance when finished.

## Project Structure

```text
.
├── src/                  # React frontend source code
├── src-tauri/            # Tauri / Rust backend source code and bundle configuration
├── public/               # Frontend static assets
├── package.json          # npm scripts and frontend dependencies
├── vite.config.ts        # Vite configuration
├── tailwind.config.js    # Tailwind CSS configuration
└── tsconfig*.json        # TypeScript configuration
```

> The project structure may change as the project evolves. Please refer to the actual source tree.

## Contributing

Issues and Pull Requests are welcome.

Before submitting changes, please consider:

- Keep the change scope clear and focused
- Follow the existing code style
- Run the necessary build or check commands
- Avoid unrelated formatting or refactoring changes

> Most of the code for this software was also written using Qwen3.6-35B-A3B, which is deployed locally.

## License

[**GNU General Public License v3.0**](../LICENSE)
